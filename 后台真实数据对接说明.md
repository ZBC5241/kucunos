# 李家村库存工作台 V10.0｜后台真实数据对接说明

> 给后续接手 AI / 开发者使用。目标：在**不破坏现有搜索准确性、17 店口径、库存判断逻辑和响应式 UI**的前提下，把当前 HTML 内嵌静态库存数据替换为后台真实数据。

## 1. 当前正式版文件

- 主文件：`李家村库存工作台_V10.0_正式上线静态版.html`
- 默认页面：`查库存`
- 响应式：手机 / 平板 / 电脑
- 当前数据：HTML 内嵌静态 `inventoryData`
- 已预留同步注入入口：`window.__LJC_INVENTORY_DATA__`

当前源码中的数据入口形态：

```js
const inventoryData = Array.isArray(window.__LJC_INVENTORY_DATA__)
  ? window.__LJC_INVENTORY_DATA__
  : [ /* 当前静态底表 */ ];
```

如果后台可以在页面主脚本执行**之前**同步注入数据，只要先设置：

```js
window.__LJC_INVENTORY_DATA__ = backendRows;
```

即可让现有前端使用新数据。

如果后台使用 `fetch()` / AJAX 异步请求，则**不能只把数组替换成 fetch**。必须让“搜索索引初始化”和“工作台 SKU 模型初始化”都等数据加载完成后再执行。

---

## 2. 库存记录字段契约

每条库存记录需要保持以下字段语义：

| 字段 | 含义 | 关键约束 |
|---|---|---|
| `code` | 商品编码 / SKU 主键 | **最重要身份字段**；不同颜色/内存必须不同 SKU 时按底表编码区分 |
| `name` | 完整 SKU 商品名称 | **禁止改简称**；必须保留型号、RAM、ROM、颜色、版本等完整信息 |
| `wh` | 门店/仓库完整名称 | 必须与 17 店白名单名称一致，才能进入正式统计 |
| `cat` | 商品分类 | 核心品类用于工作台健康度判断 |
| `q` | 账面库存 | 不能替代 `a` 作为可售库存 |
| `a` | 可用 / 可售库存 | **所有“可售库存”必须严格使用此字段；`a=0` 就是 0，禁止回退 `q`** |
| `series` | 系列辅助字段 | 搜索辅助 |
| `model` | 型号/版本辅助字段 | 搜索辅助，不能完全依赖它识别 Pro/Max 等版本 |
| `mem` | 内存/容量辅助字段 | 搜索辅助；前端还会从完整名称二次规范化 |
| `color` | 颜色辅助字段 | 搜索辅助 |
| `demo` | 是否演示机 | 演示机隔离依据之一 |
| `hw` | 原底表辅助字段 | 不可单独用来判断是否真机/终端设备 |

最低推荐返回格式：

```js
{
  code: "SKU_CODE",
  name: "完整商品名称（型号 + 内存 + 颜色 + 版本）",
  wh: "完整门店名称",
  cat: "手机",
  q: 3,
  a: 3,
  series: "Mate X7",
  model: "标准",
  mem: "12+512",
  color: "曜石黑",
  demo: false,
  hw: true
}
```

数值字段建议后台直接返回 Number；Boolean 字段必须是真布尔值，不要返回字符串 `"false"` / `"true"`。

---

## 3. 17 店正式统计白名单

前端业务模型只统计以下 17 店：

1. 华为高新金鹰授权体验店
2. 华为高新中大国际授权体验店
3. 华为高新大茂城授权体验店
4. 华为朝阳益田授权体验店
5. 华为浐灞优享授权体验店
6. 华为大兴路龙湖授权体验店
7. 华为李家村万达授权体验店
8. 华为长安万科广场授权体验店
9. 华为长乐授权体验店
10. 华为凯德广场授权体验店
11. 华为金辉环球授权体验店
12. 华为西安SKP至臻店
13. 华为智能生活馆西安荟聚
14. 华为智能生活馆西安大悦城
15. 华为智能生活馆西安大唐不夜城
16. 华为智能生活馆西安高新万达
17. 华为华阳城合作店

本店固定为：

```js
HOME = '华为李家村万达授权体验店'
```

**不要对门店名称做随意简称后再传给前端**。如果后台名称不同，应在接口适配层统一映射为以上标准名称。

---

## 4. 核心品类

核心终端品类：

```js
手机
平板电脑
穿戴
电脑
音频
智慧屏
```

工作台健康度、补货任务等默认核心视图依赖这些分类名称。

---

## 5. 库存健康度规则（不要擅自修改）

对每个商品编码独立计算，不可把不同颜色/内存 SKU 合并。

1. 取 17 店中正库存门店。
2. `ref = ceil(median(positive stock among 17 stores))`
3. `low = max(1, ceil(ref * 0.6))`
4. `high = max(ref + 2, ceil(ref * 1.6))`
5. 本店：
   - `own == 0` → 无货
   - `own < low` → 低水位
   - `own > high` → 偏高
   - 其他 → 健康
6. `suggest = max(0, ref - own)`
7. 货源门店按可售库存降序排列。

`ref` 只是当前前端门店分布参考水位，**不是正式安全库存**。

---

## 6. 搜索规则是高风险区，不建议重写

当前搜索已经针对以下问题做过严格处理：

- 设备型号搜索时，不允许手机壳 / 膜 / 保护套等配件自动混入。
- 真机无货时，不允许拿配件“补结果”。
- 演示机 / 体验机 / 样机默认与可售列表隔离，只有用户点击演示机入口后显示。
- RAM + ROM 必须按真实容量条件匹配。
- 支持 `12+512`、`16+512`、`1TB` 等容量写法。
- 支持日常连写：例如 `MateX712+256黑`、`FreeBudsPro5银`、`WatchGT6Pro黑`。
- Pro / Pro Max / Pro+ / Ultra / RS / Max / Plus / SE / Air / Mini 等功能版本严格区分。
- 典藏版、昆仑玻璃、风驰版、优享版、先锋版、活力版、NFC 版、足球款、套装礼盒等特殊款：**用户没有输入就默认不能出现**。
- `nova16` 与 `nova16z`、`PuraX` 与 `PuraX Max`、`WATCH GT6` 与 `GT6 Pro` 等边界已单独处理。
- 商品编码属于强身份字段；只搜编码时应直接按编码定位。
- 同名多编码不能偷偷合并或猜测，要保留真实 SKU 差异。

后台对接时建议：**只换数据层，不动搜索层**。如必须改搜索算法，需要重新跑全量回归。

---

## 7. 演示机判断不能只看 `demo`

原始数据中存在某些非可售记录 `demo=false`，但实际：

- `cat = 演示机`
- `wh` 带“演示机”
- `name` 带“演示机 / 样机 / 体验机 / 陈列哑机 / 哑机”

因此前端已有统一 `isNonSaleStock()` / 演示类识别逻辑。

后台最好直接把演示类记录规范为 `demo=true`，但**不要删除前端的语义兜底判断**。

---

## 8. 后台接入推荐方案

### 方案 A：服务端/页面加载前同步注入（改动最小）

在正式 HTML 主脚本执行前：

```html
<script>
window.__LJC_INVENTORY_DATA__ = YOUR_BACKEND_DATA;
</script>
```

优点：现有搜索和业务模型基本无需改造。

### 方案 B：REST API 异步加载（推荐长期方案）

建议重构成：

```js
async function bootstrap() {
  const rows = await fetchInventory();
  inventoryData = normalizeRows(rows);
  rebuildSearchIndex();
  rebuildSkuModel();
  renderInitialPage();
}
```

**注意：当前搜索索引和工作台 SKU 模型都是在脚本初始化阶段基于 `inventoryData` 构建的。**
所以异步接入必须统一 bootstrap，不能出现：

1. 页面先用旧静态数据构建索引；
2. 后台数据之后才覆盖 `inventoryData`；
3. 页面看似更新，实际搜索/统计仍用旧索引。

这是后台接入最需要避免的隐蔽 Bug。

---

## 9. 推荐 API

建议接口最少提供：

```http
GET /api/inventory
```

返回：

```json
{
  "updatedAt": "2026-08-31T13:30:00+08:00",
  "items": []
}
```

前端只使用 `items` 生成搜索索引和业务模型；`updatedAt` 更新顶部“更新于”时间。

如果后续需要调拨流程真正写回后台，再增加：

```http
POST /api/transfers
PATCH /api/transfers/:id
GET /api/transfers
```

当前静态版的调拨/任务状态存在 LocalStorage：

- `ljc-transfer-v55`
- `ljc-task-v55`

正式多人协作后建议迁移到后台数据库，否则不同设备之间状态不会同步。

---

## 10. 后台接入完成后的必须回归测试

至少重新验证：

1. 默认打开就是“查库存”。
2. 手机 320 / 360 / 390 / 430px 无横向溢出。
3. 电脑 1366px 左侧导航、顶部搜索、双列结果正常。
4. 完整 SKU 名称必须可完整看到，不能截掉内存/颜色。
5. `Mate X7 12+256 黑` 类无可售 SKU 查询不能出现手机壳。
6. `WATCH5 紫` 类颜色简称能准确命中。
7. 演示机默认隐藏，点击后才展开；换新搜索词自动收起。
8. Pro / Max / Ultra / SE 等版本互不污染。
9. 商品编码精确搜索。
10. 17 店以外仓库不得进入可售统计。
11. `a=0` 时可售库存必须为 0。
12. 同一门店同一 SKU 重复行聚合后不能重复计数。
13. 补货警示角标数量与待处理 SKU 一致。
14. 加入调拨 → 状态切换 → 移除后任务状态同步清除。

---

## 11. 当前静态版基线（用于接入后对账）

当前内嵌快照重新计算：

- 原始库存记录：13,809 行
- 17 店可参与业务模型的全品类 SKU：668
- 核心 SKU：368
- 核心状态：无货 169 / 低水位 31 / 健康 152 / 偏高 16
- 本店全品类在库 SKU：355
- 本店全品类可用库存：2,408
- 本店核心在库 SKU：199
- 本店核心可用库存：511
- 当前核心商品编码→完整名称冲突：0

后台接入后数据会变化，但如果同一静态快照接入 API，结果应该与以上基线一致。

---

## 12. 给接手 AI 的一句话任务说明

> 请在不改变 V10.0 已审计 UI、搜索规则、17 店统计口径、完整 SKU 显示规则和库存健康模型的前提下，把 HTML 内嵌 `inventoryData` 替换为真实后台数据源。先做字段映射/数据规范化，再统一初始化搜索索引与工作台 SKU 模型。任何搜索算法改动必须重新跑完整 SKU、商品编码、版本边界、配件污染、演示机隔离和响应式回归测试。
