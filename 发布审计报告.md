# 李家村库存工作台 V10.0｜发布审计报告

## 发布结论

V10.0 已封装为正式上线静态版。V10.0 相比上一视觉基线仅增加：版本标识、后台字段契约、同步数据注入钩子和运行时只读信息；**搜索算法、查询页 DOM 和全部视觉 CSS 未改动**。

## 本轮重新执行的静态审计

- HTML 文件可正常读取，大小约 4.7 MB。
- JavaScript 共 2 个脚本块，均通过 `node --check` 语法检查。
- DOM `id` 重复：0。
- `<style id>` 重复：0。
- 底部导航 5 个入口完整：工作台 / 本店 / 查库存 / 补货 / 调拨。
- 默认路由 `switchTab('query')`：1 处，首次打开明确进入“查库存”。
- 手机/平板/桌面响应式断点存在：768px、1024px。
- A 雾紫主题存在。
- 补货待处理角标保持警示红，调拨数量保持主色状态提示。
- 未发现 `a=0` 时回退 `q` 的危险库存写法。
- 后台同步数据注入钩子存在：`window.__LJC_INVENTORY_DATA__`。
- 后台字段契约存在：`window.LJC_INVENTORY_SCHEMA`。
- 运行时只读信息存在：`window.LJC_RUNTIME_INFO`。

## 静态数据结构审计

当前内嵌库存快照：

- 原始记录：13,809 行。
- 12 个必需字段缺失记录：0。
- `demo` 非 Boolean：0。
- `hw` 非 Boolean：0。
- `q` 非数值：0。
- `a` 非数值：0。

重新按正式业务规则计算：

- 全品类有效 SKU：668。
- 核心 SKU：368。
- 核心状态：健康 152 / 偏高 16 / 低水位 31 / 无货 169。
- 本店全品类在库 SKU：355。
- 本店全品类可售库存：2,408。
- 本店核心在库 SKU：199。
- 本店核心可售库存：511。
- 核心商品编码→完整名称冲突：0。

以上与正式基线一致，说明 V10.0 封装没有改变库存模型结果。

## 搜索与视觉逻辑完整性

通过源码哈希/文本对比确认：

- V10.0 搜索逻辑从 `MODEL_WORDS` 开始至查询脚本结束，与上一已审计版本完全一致。
- V10.0 查询页 DOM 与上一视觉基线完全一致。
- V10.0 全部视觉 CSS（排除新增的空发布标记）与上一视觉基线完全一致。

因此 V10.0 的发布封装没有重新引入搜索或布局变化。

## 后台接入风险控制

V10.0 新增同步数据注入方式：

```js
window.__LJC_INVENTORY_DATA__ = rows;
```

必须在页面库存主脚本执行前完成。

如果改为异步 API 请求，必须等待数据加载后再统一构建：

1. 搜索索引；
2. SKU 业务模型；
3. 页面初始渲染。

严禁“先用静态数据建索引，再异步覆盖数组”，否则可能出现页面显示新数据、搜索仍使用旧索引的隐蔽错误。

详见：`李家村库存工作台_V10.0_后台真实数据对接说明.md`。

## 文件完整性

正式 HTML SHA-256：

`a99979d77ba90de102b154656f0a8df1ce7d742ab4225e6642be0e7de092df0c`

> 注：若后续再次修改正式 HTML，SHA-256 会变化，应重新审计并更新校验值。
